{
  "name": "pflegemittelbox-mcp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix dashboard\"",
    "setup": "npm install --prefix server && npm install --prefix dashboard",
    "install:all": "npm install --prefix server && npm install --prefix dashboard"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
